{
  "name": "gitlab-merge-request-reminder",
  "version": "1.0.0",
  "description": "Sends reminders of open merge requests in GitLab",
  "main": "lib/index.js",
  "scripts": {
    "remind": "ts-node ./src/index.ts",
    "build": "tsc -p ./src -d",
    "build-docker": "(cd ./_build/scripts && docker.cmd)"
  },
  "author": "David Kendall",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/node": "^14.11.1",
    "axios": "^0.20.0",
    "moment": "^2.28.0"
  }
}
